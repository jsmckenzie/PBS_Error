{Error using reshape
Number of elements must not change. Use [] as one of the size inputs to automatically calculate the appropriate size for that dimension.

Error in Octopus_2D/reshape (line 37)
                    y = reshape(x,obj.size);

Error in MZ5_2D (line 44)
                obj.totInt = obj.reshape(obj.totInt);

Error in REIMS_HPC (line 19)
oo = MZ5_2D(filePath,fileName);

Error in REIMS_Array (line 52)
    REIMS_HPC(filePath,fileNames{n},savePath);
} 
